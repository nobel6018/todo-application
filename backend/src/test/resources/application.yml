spring:
  datasource:
    username: sa
    password:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:"
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100
    hibernate:
      ddl-auto: create-drop